package org.example;

/**
 * strategy design pattern for implementing different strategy
 *  interface of the different strategy
 */
public interface Strategy {
    public void moveToNextPos(Enemy enemy);
}
